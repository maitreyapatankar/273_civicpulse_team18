# Test-only dependencies for CivicPulse.
# Install with:  pip install -r tests/requirements-dev.txt
#
# These are intentionally separate from each service's requirements.txt so
# production images stay slim. Pinned to recent stable releases known to work
# with Python 3.11+.

pytest==8.3.3
pytest-mock==3.14.0
fakeredis==2.26.1
faker==30.8.2
httpx==0.27.2
